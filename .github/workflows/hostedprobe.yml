name: hostedprobe
on:
  workflow_dispatch:
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "RUNNER_NAME=$RUNNER_NAME"
          echo "RUNNER_ENVIRONMENT=$RUNNER_ENVIRONMENT"
